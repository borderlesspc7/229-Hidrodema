/** Módulos opcionais do menu principal. `false` = oculto no menu e URL redireciona para o menu. */
export const features = {
  meeting: false,
  marketing: false,
  /** Tela de sincronização API Seller + diretório (gestor/admin). */
  gestaoVendedores: true,
} as const;

